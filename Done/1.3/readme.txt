代码内容介绍：
1.根据要测试的地址范围，调用系统分配内存页面的函数，分配内存页面，每个页面的大小为4kb,也就是4096byte
2.将每个页面看成大小是4096byte的数组，向页面中写入内容
3.向页面中一次写入一个8byte的数组，temp = 0x0101010101010101,data[i] = temp*(i%256),会给页面写入一个有规律的数
4.读取页面内容，判断是否和写入的内容相同，从而给出内存读写的正确性结论
5.输出测试结果，给出结论
